services:
  - type: web
    name: horse-breeder
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && cd client && npm install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production